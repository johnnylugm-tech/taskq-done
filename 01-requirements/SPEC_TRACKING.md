# Specification Tracking Matrix — taskq-api

> Human-readable view of FR/NFR tracking. Status column is machine-refreshed from
> `build_traceability` at `advance-phase`; semantic columns (Spec Description /
> Intent Class / Decision Framework / Notes) are hand-filled.
> Canonical spec source: `SPEC.md` (repo root).

## Project Info
- Project Name: taskq-api
- Version: v1.0.0
- Phase: 3 (Implementation)
- Source-of-truth spec: `SPEC.md` (repo root)

## Specification Status

> **The Status column is machine-refreshed** — `advance-phase` overwrites each
> FR's Status from `build_traceability`'s live code/test scan (PENDING once no
> module exists, IN_PROGRESS once code/module exists, VERIFIED once code+test
> exist). The authoritative status is that scan / `quality_manifest.json`, NOT
> this hand-filled cell. Fill the semantic columns (Spec Description / Intent
> Class / Decision Framework / Notes); leave Status to refresh itself (a
> hand-edit is overwritten on the next advance).

### Functional Requirements

| FR ID | Spec Description | Intent Class | Decision Framework | Status | Notes |
|-------|-----------------|--------------|-------------------|--------|-------|
| FR-01 | Task resource CRUD API: POST /v1/tasks (write), GET /v1/tasks/{id} (read), GET /v1/tasks paginated cursor-based (read), DELETE /v1/tasks/{id} (admin). Validation: non-empty / ≤1000 chars / injection-char blacklist / unique name; 422/409 problem+json. Source: `SPEC.md` §3 FR-01. | api-surface | CRUD endpoints split into `api/routes/tasks.py`; business logic in `service/tasks.py`; persistence in `repository/tasks.py`. Validation via `pydantic` v2 `TaskCreate` model. Cursor pagination uses opaque cursor token over `(created_at, id)` ordering. | PENDING | AC-1.1..AC-1.8. Per `SPEC.md` §8 #4-#8. Cursor-based pagination with `limit` ceiling 200. DELETE is admin-only and atomic over task + result rows. |
| FR-02 | Task execution endpoint: POST /v1/tasks/{id}/run (write) → 202 + run_id. Runner uses `asyncio.create_subprocess_exec(*shlex.split(command))`; `shell=True` forbidden; `TASKQ_TASK_TIMEOUT`. State machine: pending → running → done/failed/timeout. Results persisted to `task_results` (v3 schema: exit_code / stdout_tail / stderr_tail / duration_ms / finished_at). GET /v1/tasks/{id}/runs newest-first (read). Source: `SPEC.md` §3 FR-02. | execution | Runner lives in `service/runner.py` as `AsyncTaskRunner` orchestrating `asyncio.TaskGroup`. State transitions are explicit; results persist via `repository/results.py`. Timeout via `asyncio.wait_for` with `process.kill()` + `await process.wait()`. | PENDING | AC-2.1..AC-2.4. High-risk module `taskq_api.service.runner` per `SPEC.md` §10. Subprocess must be introspected in tests to assert `shell=True` is never passed. |
| FR-03 | API Key authentication: every `/v1/*` requires `X-API-Key`; missing/invalid/revoked → 401 + problem+json. Keys stored as SHA-256 hashes (64 hex chars) in `api_keys.key_hash`; comparison via `hmac.compare_digest`. Plaintext printed exactly once at `python -m taskq_api key create --scope <scope>`. `/healthz` and `/readyz` exempt. Source: `SPEC.md` §3 FR-03. | security | Auth dependency in `service/auth.py` as `ApiKeyAuthenticator`; persistence in `repository/keys.py`; CLI entrypoint in `cli/key.py`. Constant-time comparison enforced via `hmac.compare_digest`. | PENDING | AC-3.1..AC-3.6. Per `SPEC.md` §8 #5 + #18. Plaintext printed once at creation, never persisted. High-risk module `taskq_api.service.auth` per `SPEC.md` §10. |
| FR-04 | Scope authorization: hierarchical `read < write < admin`. Per-endpoint required scope enforced; insufficient → 403 + problem+json; body MUST NOT disclose resource existence. Single dependency/ middleware enforces every `/v1/*` route. Source: `SPEC.md` §3 FR-04. | security | `service/auth.py::ScopeAuthorizer` is the single dependency attached to every `/v1/*` route via FastAPI `Depends`. Scope check runs BEFORE resource lookup to prevent existence leak. | PENDING | AC-4.1..AC-4.3. Per `SPEC.md` §8 #6. Route introspection test asserts exactly one scope-dependency function across all `/v1` routes. |
| FR-05 | Rate limiting: per-token token bucket. Capacity `TASKQ_RATE_BURST`; refill rate `TASKQ_RATE_PER_SEC`. Over-limit → 429 + problem+json + `Retry-After` header. Bucket state in DB (cross-worker consistency); update runs in single transaction + row-level lock. `/healthz` and `/readyz` exempt. Source: `SPEC.md` §3 FR-05. | reliability | `service/ratelimit.py::TokenBucket` consults `repository/ratelimit.py` per request. Bucket row update uses `SELECT … FOR UPDATE` (or SQLite equivalent) inside a transaction to prevent double-spend under concurrency. | PENDING | AC-5.1..AC-5.3. Per `SPEC.md` §8 #9. Concurrency test asserts no double-spend across N workers. |
| FR-06 | Persistence layer and transaction boundaries: all data access via `repository/`; one `Session` per API request; commit-on-success / rollback-on-exception via context manager. SQL string concatenation forbidden; ORM or parameterized queries only. Eager loading via `selectinload` / `joinedload` (N+1 is a failure condition). Pool: `pool_size=TASKQ_DB_POOL_SIZE`, `pool_pre_ping=True`. Source: `SPEC.md` §3 FR-06. | layering | `repository/session.py::SessionFactory` + `transactional()` context manager. `service/` and `api/` MUST NOT import `sqlalchemy` directly (enforced by `.importlinter`). High-risk module `taskq_api.repository.session` per `SPEC.md` §10. | PENDING | AC-6.1..AC-6.5. Per `SPEC.md` §8 #14 #17 #21. List endpoints must emit a CONSTANT number of SQL statements regardless of result count. |
| FR-07 | Alembic three-step schema migration: v1 (tasks, api_keys) → v2 (tags, task_tags + tasks.name unique index) → v3 (split `tasks.result_json` into `task_results` with data migration). Each revision has a working `downgrade`. Round-trip: `upgrade head` → write sample → `downgrade -1` → `upgrade head` preserves data field-by-field. `op.execute("DROP TABLE …")` forbidden as a downgrade shortcut. Source: `SPEC.md` §3 FR-07. | migration | `migrations/versions/v1_initial.py`, `v2_tags_and_unique.py`, `v3_split_results.py`. v3 uses explicit data migration (INSERT into `task_results` from `tasks.result_json`) followed by column drop; `downgrade` reverses. High-risk module `migrations.versions.v3_split_results` per `SPEC.md` §10. | PENDING | AC-7.1..AC-7.6. Per `SPEC.md` §8 #11 #12 #13. Migration tests run against a real SQLite file (NOT `:memory:`) per NFR-09. Round-trip is field-by-field equality, not row count. |
| FR-08 | Async runner: `asyncio.TaskGroup` for background execution. Graceful drain on shutdown — wait up to `TASKQ_DRAIN_TIMEOUT` for in-flight tasks; over-budget tasks marked `interrupted`. Concurrency cap `TASKQ_MAX_CONCURRENT` (excess requests queue, no unbounded coroutines). Task timeout via `asyncio.wait_for`; on timeout child MUST be killed (`process.kill()` + `await process.wait()`); no orphan processes. `asyncio.CancelledError` MUST propagate; never swallowed by `except Exception:`. Source: `SPEC.md` §3 FR-08. | execution | `service/runner.py::AsyncTaskRunner` owns the `TaskGroup`; drain logic in `service/runner.py::drain`; lifespan wiring in `api/lifespan.py`. Bounded semaphore enforces `TASKQ_MAX_CONCURRENT`. CancelledError is explicitly re-raised (never caught by `except Exception:`). High-risk module `taskq_api.service.runner` per `SPEC.md` §10. | PENDING | AC-8.1..AC-8.4. Per `SPEC.md` §8 #25. Orphan-process assertion runs against `psutil.process_iter()` or platform-equivalent. |
| FR-09 | Health & observability: `GET /healthz` (no auth) → 200 `{"status":"ok"}` when process alive; `GET /readyz` (no auth) → 200 when DB reachable AND `alembic current == head`, else 503 with failing item named; `GET /v1/metrics` (admin) → task counts by status, execution-latency percentiles, rate-limit rejection counts. `/readyz` must fail-closed on "migration not at head". Source: `SPEC.md` §3 FR-09. | observability | `api/routes/health.py::healthz` + `readyz`; `api/routes/metrics.py::metrics`; `service/healthchecks.py::alembic_head_check`. `/readyz` checks DB ping + `alembic current` (via `alembic.context.get_current_revision()` or `alembic.ini`-driven script). | PENDING | AC-9.1..AC-9.5. Per `SPEC.md` §8 #10 #11. The "migration not at head" → 503 path is the primary shipping guardrail. |
| FR-10 | RFC 7807 error contract: every non-2xx response has `Content-Type: application/problem+json`. Body fields: `type` (URI) / `title` / `status` / `detail` / `instance` / `correlation_id`. `detail` MUST NOT leak internal details (no stack/SQL/path/schema). `correlation_id` appears in both response header `X-Correlation-Id` and server log. Error-code map: 422 validation / 401 unauthenticated / 403 insufficient scope / 404 unknown resource / 409 name conflict / 429 rate-limited / 503 not ready / 500 other. `asyncio.CancelledError` is NOT in the table — it propagates. Source: `SPEC.md` §3 FR-10 + §7. | reliability | `errors/problem.py::ProblemDetail` builder; `errors/handlers/exception_handler.py` for global mapping; `api/middleware/correlation_id.py` issues the id per request. `detail` redaction is a whitelist (template strings), not a regex filter, to avoid accidental truncation of legitimate user-facing messages. | PENDING | AC-10.1..AC-10.5. Per `SPEC.md` §8 #19 + §7. Single source of truth for the error-code map (one Python dict keyed by HTTP status). |

### Non-Functional Requirements

| NFR ID | Spec Description | Intent Class | Decision Framework | Status | Notes |
|--------|-----------------|--------------|-------------------|--------|-------|
| NFR-01 | Performance and query efficiency: `GET /v1/tasks/{id}` p95 < 30ms over a 10,000-row fixture (excluding network / ASGI transport); `GET /v1/tasks?limit=50` p95 < 80ms over a 10,000-row fixture. N+1 is a failure condition: list endpoints MUST emit a CONSTANT number of SQL statements regardless of result count, asserted via SQLAlchemy event listener. Measurement: `pytest-benchmark`. Source: `SPEC.md` §4 NFR-01. | performance | Eager loading via `selectinload` / `joinedload` on relations; `(created_at, id)` composite index for cursor pagination. Benchmark fixtures seed 10k rows via `pytest-benchmark` setup. Source: `SPEC.md`. | PENDING | AC-N1.1..AC-N1.3. Per `SPEC.md` §8 #14 #15 + §11. Constant SQL count = list endpoint emits C statements for any result count N. |
| NFR-02 | HTTP and data-layer security: `shell=True`, `eval(`, `exec(` forbidden across codebase (grep = 0 hits in `03-development/src/`). SQL string concatenation forbidden (no f-string / `%` / `+` building SQL); ORM or parameterized queries only. API keys stored hashed, compared via `hmac.compare_digest`. 403 responses MUST NOT disclose resource existence. Error bodies MUST NOT contain stack/SQL/path. CORS default-denies ALL origins; allowlist is `TASKQ_CORS_ORIGINS`. `bandit -r 03-development/src/`: 0 HIGH, 0 MEDIUM. Source: `SPEC.md` §4 NFR-02. | security | Grep gates (`shell=True|eval(|exec(`, SQL concat patterns) enforced as tests. CORS middleware applied at app level with default-deny + env-var allowlist. `bandit` runs in CI. Source: `SPEC.md`. | PENDING | AC-N2.1..AC-N2.3. Per `SPEC.md` §8 #16 #17 #18 #19 #23. CORS allowlist is opt-in via env var. |
| NFR-03 | Error handling, transaction and async correctness: per-request explicit transaction boundaries (commit-on-success / rollback-on-exception) via context manager. Bare `except:` and `except Exception: pass` forbidden. `asyncio.CancelledError` MUST propagate (never swallowed). DB connection failure → `/readyz` 503 with explicit detail; infinite silent retries forbidden. Task timeout MUST terminate the subprocess (no orphans). Migration failure MUST roll back; DB remains at previous revision. Source: `SPEC.md` §4 NFR-03. | reliability | AST scan enforces no `except:` / `except Exception: pass` patterns in `03-development/src/`. `transactional()` context manager used uniformly. Cancellation handler explicitly re-raises. `/readyz` has no retry loop — surfaces failure immediately. Source: `SPEC.md`. | PENDING | AC-N3.1..AC-N3.4. Per `SPEC.md` §8 #10 + NFR-03. The "no infinite silent retry" clause is checked via static analysis, not runtime timeout. |
| NFR-04 | Sensitive data redaction: `stdout_tail` / `stderr_tail` / logs / error bodies scanned before persistence or egress; lines matching `(sk-[A-Za-z0-9_-]{8,}|token=\S+|Bearer\s+\S+|postgres(ql)?://[^\s]+)` replaced with `[REDACTED]`. DB connection strings (incl. passwords) MUST NOT appear in any log, error message, or `/v1/metrics` response. API key plaintext output exactly once at `key create`; NEVER written to any persistent location. Source: `SPEC.md` §4 NFR-04. | security | Redaction is a single `redact()` function applied at each egress boundary (DB write of `stdout_tail` / `stderr_tail`, log formatter, error-body builder, metrics aggregator). DB URL is passed via env var and never stringified into logs. Source: `SPEC.md`. | PENDING | AC-N4.1..AC-N4.3. Per `SPEC.md` §8 #20 + NFR-04. Redaction runs BEFORE persistence, not after. |
| NFR-05 | Documentation coverage: every public function/class in `03-development/src/` has a docstring including `[FR-XX]` or `[NFR-XX]` references; coverage 100%. Each `/v1/*` endpoint has `summary` and `description` in the OpenAPI schema (`/openapi.json`). Source: `SPEC.md` §4 NFR-05. | documentation | AST-based docstring-coverage tool reports missing `[FR-XX]` / `[NFR-XX]` tokens. FastAPI endpoint decorators carry `summary=` + `description=` kwargs (asserted against `/openapi.json`). Source: `SPEC.md`. | PENDING | AC-N5.1..AC-N5.2. Per `SPEC.md` NFR-05. Coverage = (public symbols with `[FR-XX]` or `[NFR-XX]` in docstring) / (total public symbols). |
| NFR-06 | Architecture layering contract: `.importlinter` at project root declares `api > service > repository > models`; upper can import lower; lower MUST NOT import upper; `config` and `errors` are independent modules. Any layer other than `repository` MUST NOT import `sqlalchemy` (ORM leakage prevention). `lint-imports` MUST exit 0. Removing `.importlinter`, wildcard `ignore_imports`, or downgrading the contract to pass is forbidden. Source: `SPEC.md` §4 NFR-06. | layering | `.importlinter` declares the four-layer contract + the `repository`-only `sqlalchemy` import. `lint-imports` runs in CI; exit code gates phase advance. The contract file's presence + non-trivial content is also a test target. Source: `SPEC.md`. | PENDING | AC-N6.1..AC-N6.3. Per `SPEC.md` §8 #21 + §11. Downgrade-to-pass is an explicit anti-pattern. |
| NFR-07 | Dependency and license compliance: all runtime deps pinned with `==` in `requirements.txt`; transitive deps fully locked via `requirements.lock`. Allowed licenses: MIT, BSD-2-Clause, BSD-3-Clause, Apache-2.0, PSF. Any other license → that dep MUST NOT be used. Scan scope MUST include the FULL dependency tree (direct + transitive); evidence command: `pip-licenses --format=json --with-system`. SBOM at `08-config/SBOM.json` containing each dep's `name` / `version` / `license` / `direct|transitive`. Source: `SPEC.md` §4 NFR-07. | licensing | `pip-licenses` CI scan asserts every dep's license ∈ allowlist. SBOM regeneration is a Makefile target. `requirements.lock` is regenerated on every dep change. Source: `SPEC.md`. | PENDING | AC-N7.1..AC-N7.3. Per `SPEC.md` §8 #22 + NFR-07. Full-tree scan is non-negotiable; transitive-only violations also block. |
| NFR-08 | Mutation testing: `.methodology/harness_config.json` sets `features.mutation_testing: true`. Mutation score ≥ 70. Scope limited to `service/` and `repository/` with budget rationale noted in `harness_config.json`. Source: `SPEC.md` §4 NFR-08. | mutation | `mutmut run` then `mutmut results`; score asserted ≥ 70. Scope + rationale stored in `harness_config.json` `features.mutation_testing.scope` + `notes` fields. Source: `SPEC.md`. | PENDING | AC-N8.1..AC-N8.2. Per `SPEC.md` §8 #24 + NFR-08. Budget figure deferred via OI-3 (NFR-99 escape). |
| NFR-09 | Verification truthfulness (zero-skip iron rule): no FR/NFR verification test may be `pytest.skip` / `skipif` / `xfail` / an assertion-less stub. `pytest 03-development/tests -q` skipped count = 0. Every test function has ≥1 `assert` (`zero_assert == 0`). Anti-fabrication: excluding tests via `--ignore` / `-k` / `--deselect` / `collect_ignore` / removing a directory from `testpaths` is forbidden. FR-07 migration tests run against a real SQLite file (NOT `:memory:`). `TRACEABILITY_MATRIX.md` `VERIFIED` is granted ONLY when the test actually ran and passed. Source: `SPEC.md` §4 NFR-09. | testability | `pytest -q` exit code + `skipped` summary count asserted = 0. AST scan finds zero `assert`-less test functions. Migration tests use `tmp_path / "test.db"` (real file), not `:memory:`. `TRACEABILITY_MATRIX.md` `VERIFIED` cell is generated from `build_traceability`, not hand-typed. Source: `SPEC.md`. | PENDING | AC-N9.1..AC-N9.4. Per `SPEC.md` §8 #1 + NFR-09 + §11. This is the round-2 anchor dimension. |
| NFR-10 | Integration coverage: `03-development/tests/integration/` line coverage ≥ 80%. Integration tests driven by `httpx.AsyncClient(transport=ASGITransport(app))`; calling handler functions directly is forbidden. Must cover at minimum: full CRUD chain; each of 401/403/404/409/422/429/503 with one example; migration round-trip; rate-limit triggering and recovery; graceful drain. Source: `SPEC.md` §4 NFR-10. | integration | `pytest-cov` run with `--cov=03-development/src` and integration-only path. ASGITransport usage is asserted via AST scan (no direct handler-call imports). Per-error-code coverage is a per-FR test that asserts the integration suite contains ≥1 example per code. Source: `SPEC.md`. | PENDING | AC-N10.1..AC-N10.3. Per `SPEC.md` §8 #3 + NFR-10. The "no direct handler call" rule applies to integration tests, not unit tests. |
| NFR-11 | Readability / maintainability: project MI (LLOC-weighted) ≥ 80; per-function CC ≤ 10; single file ≤ 400 lines; single directory ≤ 15 files; each API handler ≤ 40 lines (business logic pushed down to `service/`). Source: `SPEC.md` §4 NFR-11. | maintainability | `radon mi -s` reports project MI; `radon cc -s` reports per-function CC. File/dir size asserted via `find … | xargs wc -l` or equivalent. Handler-length check uses AST per-FunctionDef LOC. Source: `SPEC.md`. | PENDING | AC-N11.1..AC-N11.3. Per `SPEC.md` §11 + NFR-11. Vocabulary uses `maintainability` (not `readability`) per pinned vocabulary list. |
| NFR-12 | System verification goal: `Makefile` `verify-system` target chains: (1) `alembic upgrade head`; (2) full test suite; (3) service startup + `/healthz`, `/readyz` smoke; (4) `alembic downgrade base` then `alembic upgrade head` (round-trip verification). `make verify-system` MUST exit 0 and print `verify-system: PASS` on stdout. Source: `SPEC.md` §4 NFR-12. | verifiability | Makefile target orchestrates the four steps in order. Exit code captured. Stdout literal `verify-system: PASS` asserted via test. The chain order is a deliverable property (not just "all four ran"). Source: `SPEC.md`. | PENDING | AC-N12.1..AC-N12.2. Per `SPEC.md` §8 #27 + NFR-12. This is the single command that proves end-to-end system integrity. |

## FR/NFR Coverage Index

The SRS defines 10 FRs (FR-01..FR-10) and 12 NFRs (NFR-01..NFR-12). Every entry above corresponds 1:1 to a heading in `SPEC.md` §3 / §4 (canonical) and to a section in `01-requirements/SRS.md`. Source-of-truth citations point to bare `SPEC.md` (repo root); the harness `check_forward_refs` gate enforces that the canonical spec lives at the repo root (no nested copy under `01-requirements/`).

- Total FRs: 10 (FR-01..FR-10)
- Total NFRs: 12 (NFR-01..NFR-12)
- Total ACs: 49 FR-ACs + 35 NFR-ACs = 84 stable identifiers of the form `AC-<n>.<m>` / `AC-N<n>.<m>` (per `01-requirements/SRS.md` §5).

## Update log

| Date | Change | By |
|------|--------|----|
| 2026-09-06 | Initial population from `01-requirements/SRS.md` (10 FRs + 12 NFRs). Standard template columns only; Status left to machine refresh from `build_traceability`. Canonical spec citations use bare `SPEC.md` (repo root). | Agent A (Requirements Engineer) |